name: Deploy Kapsalon frontend

on:
  push:
    branches:
      - main
    paths:
      - 'apps/kapsalon/apps/web/**'
      - 'scripts/sync-kapsalon-frontend.sh'
      - 'scripts/sync-static-site.sh'
      - 'Makefile'
      - '.github/workflows/deploy-kapsalon-frontend.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment environment
        required: true
        default: Development
        type: choice
        options:
          - Development
          - Staging
          - Production

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'eu-central-1' }}

jobs:
  resolve-targets:
    runs-on: ubuntu-latest
    outputs:
      environments: ${{ steps.resolve.outputs.environments }}
    steps:
      - name: Resolve deployment environments
        id: resolve
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ENVIRONMENTS='["${{ inputs.environment }}"]'
          elif [ "${{ vars.DEPLOY_KAPSALON_FRONTEND_ALL_ENVS }}" = "true" ]; then
            ENVIRONMENTS='["Development","Staging","Production"]'
          else
            ENVIRONMENTS='["Development"]'
          fi
          echo "environments=${ENVIRONMENTS}" >> "$GITHUB_OUTPUT"

  deploy:
    needs: resolve-targets
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.resolve-targets.outputs.environments) }}
    environment: ${{ matrix.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '26'
          cache: npm
          cache-dependency-path: apps/kapsalon/apps/web/package-lock.json

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync frontend
        env:
          DEPLOY_ENV: ${{ matrix.environment }}
        run: |
          chmod +x scripts/*.sh
          case "$DEPLOY_ENV" in
            Development) make sync-kapsalon-frontend-dev ;;
            Staging) make sync-kapsalon-frontend-staging ;;
            Production) make sync-kapsalon-frontend-prod ;;
            *) echo "Unknown environment: $DEPLOY_ENV" && exit 1 ;;
          esac
