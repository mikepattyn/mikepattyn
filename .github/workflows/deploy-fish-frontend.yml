name: Deploy Fish frontend

on:
  push:
    branches:
      - main
    paths:
      - 'apps/fishi-tracking-app/mobile/**'
      - 'scripts/build-fish-web.sh'
      - 'scripts/sync-fish-frontend.sh'
      - 'scripts/sync-static-site.sh'
      - 'Makefile'
      - '.github/workflows/deploy-fish-frontend.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: Deployment environment
        required: true
        default: Development
        type: choice
        options:
          - Development
          - Staging
          - Production

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION || 'eu-central-1' }}

jobs:
  resolve-targets:
    runs-on: ubuntu-latest
    outputs:
      environments: ${{ steps.resolve.outputs.environments }}
    steps:
      - name: Resolve deployment environments
        id: resolve
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ENVIRONMENTS='["${{ inputs.environment }}"]'
          elif [ "${{ vars.DEPLOY_FISH_FRONTEND_ALL_ENVS }}" = "true" ]; then
            ENVIRONMENTS='["Development","Staging","Production"]'
          else
            ENVIRONMENTS='["Development"]'
          fi
          echo "environments=${ENVIRONMENTS}" >> "$GITHUB_OUTPUT"

  deploy:
    needs: resolve-targets
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        environment: ${{ fromJson(needs.resolve-targets.outputs.environments) }}
    environment: ${{ matrix.environment }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ hashFiles('apps/fishi-tracking-app/mobile/pubspec.lock') }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync frontend
        env:
          DEPLOY_ENV: ${{ matrix.environment }}
        run: |
          chmod +x scripts/*.sh
          case "$DEPLOY_ENV" in
            Development) make sync-fish-frontend-dev ;;
            Staging) make sync-fish-frontend-staging ;;
            Production) make sync-fish-frontend-prod ;;
            *) echo "Unknown environment: $DEPLOY_ENV" && exit 1 ;;
          esac
